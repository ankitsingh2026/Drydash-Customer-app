import { Text, View } from "react-native";

export default function PickupMap() {
  return (
    <View
      style={{
        height: 180,
        alignItems: "center",
        justifyContent: "center",
        backgroundColor: "#E5E7EB",
      }}
    >
      <Text>Map available on mobile app only</Text>
    </View>
  );
}
